import { SuggestDirective } from './suggest.directive';

describe('SuggestDirective', () => {
  it('should create an instance', () => {
    const directive = new SuggestDirective();
    expect(directive).toBeTruthy();
  });
});
